# TECHNOVA Backend Dependencies

# ── Core ──
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0
python-multipart==0.0.9

# ── Database ──
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.0

# ── Security ──
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# ── Validation ──
email-validator==2.2.0

# ── Testing ──
pytest==8.3.0
pytest-asyncio==0.24.0
httpx==0.27.0
pytest-cov==5.0.0

# ── Development ──
black==24.8.0
ruff==0.6.0
mypy==1.11.0

# ── Future (commented out until needed) ──
# motor==3.5.0          # MongoDB async driver
# redis[hiredis]==5.1.0  # Redis async client
# boto3==1.35.0          # AWS S3 / Object storage
